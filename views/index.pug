extends layout

block content
  
  h1 Books

  a(href="books/new") Create new book

  table 
    th Title 
    th Author 
    th Year 
    th Genre
      
    each book in books
      tr
        td #{book.title}
        td #{book.author}
        td #{book.year}
        td #{book.genre}


